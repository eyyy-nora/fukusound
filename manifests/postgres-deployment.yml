apiVersion: apps/v1
kind: Deployment
metadata:
  name: fukusound-postgres-deployment
  labels:
    app: fukusound
    ver: postgres-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fukusound
      ver: postgres-deployment
  template:
    metadata:
      labels:
        app: fukusound
        ver: postgres-deployment
    spec:
      containers:
        - name: fukusound-postgres
          image: postgres:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5324
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: fukusound
                  key: pg_username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fukusound
                  key: pg_password
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: fukusound
                  key: pg_db
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: fukusound_pgdatavol
      volumes:
        - name: fukusound_pgdatavol
          hostPath:
            path: /opt/fukusound/data
